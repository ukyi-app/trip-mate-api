{
  "regressionCmd": "bun install --frozen-lockfile >/dev/null 2>&1 && bunx vitest run tests/regression/expenses-mutation-authz.test.ts",
  "characterizationCmd": "bun install --frozen-lockfile >/dev/null 2>&1 && bunx vitest run src/modules/expenses src/modules/expense-drafts",
  "flips": [
    {
      "testId": "tests/regression/expenses-mutation-authz.test.ts :: 비인가 actor 지출 PATCH·DELETE → 403 (open·finalized·stale 상태 무관)",
      "symptomToken": "G3-AUTHZ"
    }
  ],
  "scope": ["src/modules/expenses/**"],
  "red": { "sha": "e469e4b54c37bc5e8fdd4cb584126d5fba67697b" },
  "green": { "sha": null }
}
