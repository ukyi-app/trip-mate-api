# 봉인(prod) 소스. .env로 복사해 prod 값 채우고 `bun run secret:seal`
#   → deploy/trip-mate-api-secrets.sealed.yaml (create-app이 읽는 봉인본).
# 로컬 실행은 .env.local에서 오버라이드(Bun이 .env 위에 .env.local을 덮어씀).
# ⚠️ DB/Redis conn URL은 .env(봉인)에 넣지 말 것 — prod는 conn 핸들(db/cache-trip-mate-conn)이
#    envFrom으로 자동 주입. 로컬 conn은 .env.local에.

# ── prod 값(봉인 대상) ──
BETTER_AUTH_SECRET=                                  # openssl rand -base64 32 (32자+)
BETTER_AUTH_URL=https://trip-mate-api.ukyi.app       # 공개 host
WEB_ORIGINS=                                         # 프론트 origin(콤마구분, 예 https://trip-mate.ukyi.app)
USE_SECURE_COOKIES=true
INVITE_TOKEN_TTL_HOURS=168
OXR_APP_ID=                                          # 자동환율(선택)
# CURRENCYAPI_KEY=                                   # 자동환율 폴백(선택)
# GOOGLE_CLIENT_ID=                                  # 소셜 로그인(선택)
# GOOGLE_CLIENT_SECRET=
# RESEND_API_KEY=                                    # 초대 이메일(선택, 없으면 발송 skip)
# MAIL_FROM=noreply@ukyi.app                         # Resend 검증 발신 도메인(기본값 존재)
# FILES_BASE_URL=https://files.home.ukyi.app         # 영수증 파일 서버(선택, 없으면 영수증 라우트 미등록)
# FILES_API_KEY=                                     # files 서버 API 키(Bearer)
# FILES_BUCKET=trip-mate                             # 버킷(기본값 존재)
# USAGE_PARSER_ENGINE=codex                          # 사용내역 파싱 엔진(codex=구독 CLI·claude=API, 미설정+키 없으면 503) ⚠️ FE 고지 UI 배포 전 활성 금지
# ANTHROPIC_API_KEY=                                 # claude 엔진용 API 키(codex 엔진이면 불필요)
# USAGE_PARSER_CODEX_AUTH=                           # codex 엔진 인증(~/.codex/auth.json 원문 1줄, 부팅 시 /tmp에 materialize). 로컬은 호스트 ~/.codex 사용 시 불요

# ── 로컬 오버라이드 → .env.local (gitignore, 봉인 안 됨) ──
# BETTER_AUTH_URL=http://localhost:3000
# USE_SECURE_COOKIES=false
# TRIP_MATE_DATABASE_URL=postgres://trip:trip@localhost:5432/trip_mate
# TRIP_MATE_MIGRATE_DATABASE_URL=
# TRIP_MATE_REDIS_URL=redis://localhost:6379
